# 🗄️ Настройка базы данных и аутентификации

## Шаг 1: Настройка Google OAuth (Опционально)

### Получение Google Client ID и Secret:

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Перейдите в "APIs & Services" → "Credentials"
4. Нажмите "Create Credentials" → "OAuth client ID"
5. Выберите "Web application"
6. Добавьте Authorized redirect URIs:
   - `http://localhost:3000/api/auth/callback/google`
7. Скопируйте Client ID и Client Secret

### Настройка переменных окружения:

Создайте файл `.env` в корне проекта:

```env
# База данных (SQLite для разработки)
DATABASE_URL="file:./dev.db"

# NextAuth (обязательно измените secret в продакшене!)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="ваш-секретный-ключ-измените-в-продакшене"

# Google OAuth
GOOGLE_CLIENT_ID="ваш-google-client-id"
GOOGLE_CLIENT_SECRET="ваш-google-client-secret"

# Email для OTP кодов (используйте Gmail App Password)
EMAIL_SERVER_USER="your-email@gmail.com"
EMAIL_SERVER_PASSWORD="ваш-app-password"
EMAIL_SERVER_HOST="smtp.gmail.com"
EMAIL_SERVER_PORT="587"
EMAIL_FROM="noreply@pylearn.com"
```

## Шаг 2: Настройка Gmail для отправки OTP

### Получение App Password для Gmail:

1. Включите 2-факторную аутентификацию в Google аккаунте
2. Перейдите: https://myaccount.google.com/apppasswords
3. Выберите "Mail" и устройство
4. Скопируйте сгенерированный 16-значный пароль
5. Используйте этот пароль в `EMAIL_SERVER_PASSWORD`

## Шаг 3: Инициализация базы данных

```bash
# Установите зависимости (если еще не установлены)
npm install

# Сгенерируйте Prisma Client
npx prisma generate

# Создайте базу данных и таблицы
npx prisma db push

# (Опционально) Откройте Prisma Studio для просмотра данных
npx prisma studio
```

## Шаг 4: Запуск приложения

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

## 🎯 Возможности аутентификации

### Регистрация через Email:
1. Перейдите на `/auth/signup`
2. Введите имя, email и пароль
3. Получите OTP код на email (6 цифр)
4. Введите код для подтверждения
5. Автоматический вход после верификации

### Вход через Email:
1. Перейдите на `/auth/signin`
2. Введите email и пароль
3. Или используйте "Войти с OTP кодом"

### Вход через Google:
1. Нажмите "Войти через Google"
2. Выберите Google аккаунт
3. Автоматическое создание аккаунта при первом входе

## 📊 База данных

Приложение использует SQLite для разработки. В продакшене рекомендуется PostgreSQL:

### Переход на PostgreSQL:

```env
# В .env файле
DATABASE_URL="postgresql://user:password@localhost:5432/pylearn"
```

```bash
# Обновите schema.prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

# Примените миграции
npx prisma migrate dev
```

## 🔐 Безопасность

### Продакшен настройки:

1. **NEXTAUTH_SECRET**: Сгенерируйте случайный ключ:
   ```bash
   openssl rand -base64 32
   ```

2. **Домен**: Обновите `NEXTAUTH_URL` на ваш домен:
   ```env
   NEXTAUTH_URL="https://yourdomain.com"
   ```

3. **Google OAuth**: Добавьте продакшен redirect URI:
   ```
   https://yourdomain.com/api/auth/callback/google
   ```

## 🗄️ Структура базы данных

### Таблицы:

- **users** - Пользователи
- **accounts** - OAuth аккаунты (Google)
- **sessions** - Сессии пользователей
- **otp_codes** - OTP коды для верификации
- **lesson_progress** - Прогресс по урокам
- **completed_exercises** - Выполненные упражнения

### Просмотр данных:

```bash
npx prisma studio
```

Откроется веб-интерфейс для просмотра и редактирования данных.

## ⚠️ Важные примечания

### Выполнение Python кода:

Текущая реализация использует **упрощенную симуляцию** выполнения кода.
Для продакшена рекомендуется:

1. **Pyodide** (выполнение в браузере)
2. **Backend API** с Docker контейнерами
3. **AWS Lambda** или подобные сервисы
4. **Judge0 API** для изолированного выполнения

### Безопасность выполнения кода:

⚠️ **НИКОГДА** не используйте `eval()` или `exec()` на сервере!

Рекомендуемые решения:
- Изолированные Docker контейнеры
- Timeout ограничения
- Memory limits
- Sandboxed окружение

## 📝 Полезные команды

```bash
# Просмотр базы данных
npx prisma studio

# Сброс базы данных
npx prisma migrate reset

# Создание миграции
npx prisma migrate dev --name migration_name

# Форматирование schema
npx prisma format

# Проверка схемы
npx prisma validate
```

## 🚀 Готово!

Теперь у вас есть полнофункциональная система:
- ✅ Регистрация с OTP верификацией
- ✅ Вход через email/пароль
- ✅ Вход через Google
- ✅ Сохранение прогресса
- ✅ Проверка упражнений
- ✅ Отслеживание выполненных заданий

**Начните обучение Python с PyLearn! 🐍✨**

