# 🚀 Инструкция по деплою сайта в интернет

## Вариант 1: Vercel (Рекомендуется - БЕСПЛАТНО) ⭐

### Шаг 1: Подготовка базы данных

Для продакшена нужна PostgreSQL вместо SQLite.

#### 1.1. Создайте бесплатную PostgreSQL базу на Vercel:
1. Зарегистрируйтесь на [vercel.com](https://vercel.com)
2. Создайте новый проект
3. Перейдите в Storage → Create Database → Postgres
4. Скопируйте DATABASE_URL (будет выглядеть так: `postgresql://...`)

#### 1.2. ИЛИ используйте Supabase (тоже бесплатно):
1. Зарегистрируйтесь на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Settings → Database → Connection String → URI
4. Скопируйте строку подключения

### Шаг 2: Обновите schema.prisma

```prisma
datasource db {
  provider = "postgresql"  // Меняем sqlite на postgresql
  url      = env("DATABASE_URL")
}
```

### Шаг 3: Создайте GitHub репозиторий

```bash
# В командной строке:
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

**ВАЖНО:** Создайте `.gitignore` (если еще нет):
```
.env
.env.local
node_modules
.next
dist
.DS_Store
*.log
```

### Шаг 4: Деплой на Vercel

1. **Зайдите на [vercel.com](https://vercel.com)**
2. **Нажмите "Add New" → "Project"**
3. **Импортируйте ваш GitHub репозиторий**
4. **Настройте Environment Variables:**

Добавьте все переменные из вашего `.env` файла:

```bash
# Database
DATABASE_URL=postgresql://... (ваша Postgres URL)

# NextAuth
NEXTAUTH_URL=https://ваш-сайт.vercel.app
NEXTAUTH_SECRET=ваш-секретный-ключ (создайте новый)

# Google OAuth (если используете)
GOOGLE_CLIENT_ID=ваш-client-id
GOOGLE_CLIENT_SECRET=ваш-secret

# Email (если используете)
EMAIL_SERVER_USER=ваш-email@gmail.com
EMAIL_SERVER_PASSWORD=ваш-app-password
EMAIL_SERVER_HOST=smtp.gmail.com
EMAIL_SERVER_PORT=587
EMAIL_FROM=ваш-email@gmail.com
```

5. **Нажмите "Deploy"**

### Шаг 5: Настройте Google OAuth (если используете)

1. Зайдите в [Google Cloud Console](https://console.cloud.google.com)
2. Перейдите в ваш проект → APIs & Services → Credentials
3. Найдите ваш OAuth 2.0 Client ID
4. Добавьте в "Authorized redirect URIs":
   ```
   https://ваш-сайт.vercel.app/api/auth/callback/google
   ```
5. Добавьте в "Authorized JavaScript origins":
   ```
   https://ваш-сайт.vercel.app
   ```

### Шаг 6: Примените миграции базы данных

После первого деплоя, выполните миграции:

1. В Vercel перейдите в Settings → Functions
2. ИЛИ локально:
```bash
# Обновите DATABASE_URL в .env на продакшн URL
npx prisma db push
# ИЛИ
npx prisma migrate deploy
```

### Шаг 7: Готово! 🎉

Ваш сайт доступен по адресу: `https://ваш-сайт.vercel.app`

---

## Вариант 2: Netlify (Альтернатива)

1. Зарегистрируйтесь на [netlify.com](https://netlify.com)
2. Импортируйте GitHub репозиторий
3. Build settings:
   - Build command: `npm run build`
   - Publish directory: `.next`
4. Добавьте Environment Variables (как в Vercel)
5. Deploy

---

## Вариант 3: Railway (Хорошо для более сложных проектов)

1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. New Project → Deploy from GitHub
3. Добавьте PostgreSQL сервис
4. Настройте Environment Variables
5. Deploy

---

## 🔧 Полезные команды перед деплоем

### Проверьте проект локально:

```bash
# Production build
npm run build

# Запустите production версию
npm start
```

### Создайте NEXTAUTH_SECRET:

```bash
# В командной строке:
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

---

## ⚠️ Важные моменты

### 1. Переменные окружения
- **НИКОГДА** не коммитьте `.env` файл в Git
- Добавьте все переменные в настройках платформы

### 2. База данных
- SQLite не работает на Vercel (только для разработки)
- Используйте PostgreSQL для продакшена

### 3. Google OAuth
- Обязательно добавьте redirect URLs вашего продакшн домена
- Обновите `NEXTAUTH_URL` на продакшн URL

### 4. Email
- Убедитесь что SMTP настроен правильно
- Используйте App Passwords для Gmail

---

## 📊 Сравнение платформ

| Платформа | Бесплатный план | База данных | Сложность |
|-----------|----------------|-------------|-----------|
| **Vercel** | ✅ Отлично | ✅ PostgreSQL встроен | ⭐ Очень просто |
| **Netlify** | ✅ Хорошо | ❌ Нужна отдельно | ⭐⭐ Просто |
| **Railway** | ✅ Ограниченно | ✅ Встроена | ⭐⭐ Средне |

---

## 🆘 Проблемы и решения

### "Module not found" ошибка
```bash
npm install --legacy-peer-deps
```

### Ошибки с Prisma
```bash
npx prisma generate
npx prisma db push
```

### Ошибки с базой данных на Vercel
- Убедитесь что `provider = "postgresql"` в schema.prisma
- Проверьте что DATABASE_URL правильный

---

## 📝 Чеклист перед деплоем

- [ ] `.gitignore` настроен (не коммитить .env)
- [ ] schema.prisma использует postgresql
- [ ] Создан GitHub репозиторий
- [ ] Получена PostgreSQL база данных
- [ ] Все Environment Variables подготовлены
- [ ] Google OAuth redirect URIs обновлены
- [ ] Проект собирается локально (`npm run build`)
- [ ] NEXTAUTH_SECRET сгенерирован

---

## 🎯 Рекомендация

**Для вашего проекта лучше всего подходит Vercel:**
- ✅ Бесплатный план очень щедрый
- ✅ Идеально работает с Next.js
- ✅ Встроенная PostgreSQL база
- ✅ Автоматические деплои при push в GitHub
- ✅ Простая настройка
- ✅ Отличная производительность
- ✅ HTTPS сразу работает
- ✅ Можно подключить свой домен

---

## 🌐 После деплоя

Ваш сайт будет доступен 24/7 по красивому URL типа:
- `https://python-learning-app.vercel.app`

Можете поделиться им с друзьями, добавить в портфолио, использовать для обучения!

---

**Нужна помощь?** Просто спросите меня на любом этапе! 🚀

