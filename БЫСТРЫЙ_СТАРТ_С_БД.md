# ⚡ Быстрый старт с базой данных и аутентификацией

## 🎯 Что нужно сделать

### 1️⃣ Установить зависимости

```powershell
npm install
```

Это установит все новые пакеты:
- @prisma/client - Клиент для работы с БД
- next-auth - Аутентификация
- @next-auth/prisma-adapter - Адаптер для Prisma
- nodemailer - Отправка email
- bcryptjs - Хеширование паролей
- prisma - CLI для работы с БД

### 2️⃣ Создать файл .env

Создайте файл `.env` в корне проекта:

```env
# База данных (SQLite для начала)
DATABASE_URL="file:./dev.db"

# NextAuth (обязательно!)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="pylearn-super-secret-key-123456789"

# Google OAuth (ОПЦИОНАЛЬНО - можно пропустить)
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Email (ОПЦИОНАЛЬНО - можно пропустить)
EMAIL_SERVER_USER="your-email@gmail.com"
EMAIL_SERVER_PASSWORD="your-app-password"
EMAIL_SERVER_HOST="smtp.gmail.com"
EMAIL_SERVER_PORT="587"
EMAIL_FROM="noreply@pylearn.com"
```

**ВАЖНО**: Можно начать БЕЗ Google OAuth и Email - они опциональны!

### 3️⃣ Инициализировать базу данных

```powershell
# Сгенерировать Prisma Client
npx prisma generate

# Создать таблицы в базе данных
npx prisma db push
```

Это создаст файл `prisma/dev.db` - вашу SQLite базу данных!

### 4️⃣ Запустить приложение

```powershell
npm run dev
```

Готово! Откройте http://localhost:3000

## 🎨 Что изменилось

### Главная страница
- Теперь показывает статус входа
- Кнопка "Войти" если не авторизован
- Имя пользователя и кнопка "Выйти" если авторизован

### Новые страницы
- **/auth/signin** - Вход в аккаунт
- **/auth/signup** - Регистрация
- **/auth/error** - Ошибки входа

### Практика (/progress)
- Требует авторизации
- Проверяет решения с тестами
- Сохраняет прогресс в БД
- Показывает результаты тестов

## 🔥 Как использовать

### Вариант 1: Регистрация с Email (требует настройку Email)

1. Откройте http://localhost:3000/auth/signup
2. Введите имя, email, пароль
3. Получите OTP код на email
4. Введите код - готово!

### Вариант 2: Регистрация с Google (требует настройку Google)

1. Откройте http://localhost:3000/auth/signup
2. Нажмите "Регистрация через Google"
3. Выберите аккаунт - готово!

### Вариант 3: Создать пользователя вручную (БЕЗ настройки!)

```powershell
# Откройте Prisma Studio
npx prisma studio
```

В браузере откроется http://localhost:5555

1. Откройте таблицу "users"
2. Нажмите "Add record"
3. Заполните:
   - email: test@example.com
   - name: Тестовый пользователь
   - emailVerified: (текущая дата)
4. Сохраните

Теперь можете "войти" (NextAuth создаст сессию автоматически)

## 📊 Просмотр базы данных

```powershell
npx prisma studio
```

Откроется веб-интерфейс для просмотра:
- users - Пользователи
- lesson_progress - Прогресс по урокам
- completed_exercises - Выполненные задачи
- sessions - Активные сессии
- И другие таблицы

## 🎯 Основные функции

### ✅ Работает БЕЗ настройки:
- База данных SQLite
- Редактор кода Monaco
- Выполнение кода (симуляция)
- Проверка решений
- Сохранение прогресса
- Все уроки и алгоритмы

### ⚙️ Требует настройку:
- Google OAuth (опционально)
- Email OTP коды (опционально)
- Реальное выполнение Python кода (будущее)

## 💡 Советы

### Начните просто:
1. Установите зависимости
2. Создайте .env с минимальными настройками
3. Запустите `npx prisma db push`
4. Запустите приложение
5. Используйте Prisma Studio для создания тестового пользователя

### Затем добавьте:
- Google OAuth (если нужно)
- Email для OTP (если нужно)
- PostgreSQL (если планируете продакшен)

## 🐛 Частые проблемы

### "Prisma Client не найден"
```powershell
npx prisma generate
```

### "База данных не найдена"
```powershell
npx prisma db push
```

### "Session undefined"
Убедитесь что NEXTAUTH_SECRET установлен в .env

### "Cannot find module @prisma/client"
```powershell
npm install
npx prisma generate
```

## 📚 Дополнительные ресурсы

- **SETUP_DATABASE.md** - Подробная настройка БД и OAuth
- **FEATURES.md** - Все возможности приложения
- **ФИНАЛЬНЫЙ_ОТЧЕТ.md** - Полный отчет о реализации
- **README.md** - Общая документация

## 🎉 Готово!

Теперь у вас есть:
- ✅ База данных SQLite
- ✅ Система аутентификации
- ✅ Сохранение прогресса
- ✅ Проверка решений
- ✅ Интерактивный редактор

**Начните изучать Python с PyLearn! 🐍✨**

---

### Минимальная команда для старта:

```powershell
npm install
npx prisma generate
npx prisma db push
npm run dev
```

**Всего 4 команды - и всё работает! 🚀**

