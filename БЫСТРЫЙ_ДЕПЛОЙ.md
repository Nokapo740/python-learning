# ⚡ Быстрый деплой на Vercel (5 минут)

## 🚀 Самый простой способ загрузить сайт в интернет

### Шаг 1: Создайте GitHub аккаунт (если нет)
👉 [github.com](https://github.com) - регистрация бесплатная

### Шаг 2: Установите Git (если нет)
👉 [git-scm.com/download](https://git-scm.com/download)

### Шаг 3: Загрузите код на GitHub

Откройте PowerShell в папке проекта и выполните:

```powershell
# 1. Инициализируйте Git репозиторий
git init

# 2. Добавьте все файлы
git add .

# 3. Создайте первый коммит
git commit -m "Мой проект Python Learning"

# 4. Переименуйте ветку в main
git branch -M main

# 5. Создайте репозиторий на GitHub.com (через браузер)
# Затем подключите его:
git remote add origin https://github.com/ВАШ-USERNAME/python-learning.git

# 6. Загрузите код
git push -u origin main
```

### Шаг 4: Подготовьте базу данных

**Vercel предоставляет БЕСПЛАТНУЮ PostgreSQL:**

1. Зайдите на [vercel.com](https://vercel.com)
2. Зарегистрируйтесь (можно через GitHub)
3. Dashboard → Storage → Create Database → Postgres
4. Выберите бесплатный план
5. Скопируйте `POSTGRES_URL` (понадобится позже)

### Шаг 5: Обновите schema.prisma для продакшена

**ПЕРЕД ДЕПЛОЕМ** замените содержимое `prisma/schema.prisma`:

Скопируйте из файла `prisma/schema-production.prisma` в `prisma/schema.prisma`

ИЛИ просто измените строку 8:
```prisma
datasource db {
  provider = "postgresql"  // было: sqlite
  url      = env("DATABASE_URL")
}
```

### Шаг 6: Деплой на Vercel

1. **Зайдите на [vercel.com](https://vercel.com)**

2. **Нажмите "Add New" → "Project"**

3. **Выберите ваш GitHub репозиторий**

4. **Настройте Environment Variables** (в настройках проекта):

```env
# База данных (из Шага 4)
DATABASE_URL=postgresql://...ваша-url-из-vercel-postgres...

# NextAuth (ОБЯЗАТЕЛЬНО!)
NEXTAUTH_URL=https://ваш-проект.vercel.app
NEXTAUTH_SECRET=сгенерируйте-секрет-ниже

# Google OAuth (если используете)
GOOGLE_CLIENT_ID=ваш-client-id
GOOGLE_CLIENT_SECRET=ваш-secret

# Email (опционально)
EMAIL_SERVER_USER=ваш-email@gmail.com
EMAIL_SERVER_PASSWORD=ваш-app-password
EMAIL_SERVER_HOST=smtp.gmail.com
EMAIL_SERVER_PORT=587
EMAIL_FROM=ваш-email@gmail.com
```

**Как сгенерировать NEXTAUTH_SECRET:**

В PowerShell выполните:
```powershell
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

5. **Нажмите "Deploy"** ⚡

### Шаг 7: Настройте Google OAuth

После первого деплоя:

1. Откройте [Google Cloud Console](https://console.cloud.google.com)
2. Выберите ваш проект
3. APIs & Services → Credentials
4. Найдите OAuth 2.0 Client ID
5. Добавьте **Authorized redirect URIs**:
   ```
   https://ваш-проект.vercel.app/api/auth/callback/google
   ```
6. Добавьте **Authorized JavaScript origins**:
   ```
   https://ваш-проект.vercel.app
   ```

### Шаг 8: Готово! 🎉

Ваш сайт работает на:
```
https://ваш-проект.vercel.app
```

---

## 🔄 Как обновлять сайт

Каждый раз когда вы меняете код:

```powershell
git add .
git commit -m "Описание изменений"
git push
```

Vercel автоматически обновит сайт! ✨

---

## ⚠️ Важные моменты

### ✅ Что должно быть в .gitignore:
```
.env
.env.local
node_modules/
.next/
dist/
*.log
.DS_Store
```

### ❌ НЕ загружайте на GitHub:
- Файл `.env` с паролями
- Папку `node_modules`
- Папку `.next`

---

## 🆘 Если что-то пошло не так

### Ошибка "Build failed"
```powershell
# Проверьте что проект собирается локально:
npm run build
```

### Ошибка с Prisma
В Vercel Settings → Functions добавьте:
```
npx prisma generate && npx prisma db push
```

### Ошибка "Cannot find module"
```powershell
rm -rf node_modules
rm package-lock.json
npm install --legacy-peer-deps
```

---

## 💰 Стоимость

**БЕСПЛАТНО:**
- Vercel Hobby Plan (хватит на много проектов)
- PostgreSQL база (500MB)
- 100GB bandwidth в месяц
- Неограниченные деплои
- Автоматический HTTPS

---

## 🎯 Чеклист

- [ ] GitHub репозиторий создан
- [ ] Код загружен на GitHub
- [ ] Vercel аккаунт создан
- [ ] PostgreSQL база создана
- [ ] `schema.prisma` использует postgresql
- [ ] Environment Variables настроены
- [ ] NEXTAUTH_SECRET сгенерирован
- [ ] Google OAuth redirect URIs обновлены
- [ ] Сайт задеплоен ✅

---

## 📱 Бонус: Подключите свой домен

После деплоя в Vercel:
1. Settings → Domains
2. Добавьте свой домен (например: `python-learning.ru`)
3. Следуйте инструкциям Vercel

Можно купить домен на:
- [namecheap.com](https://namecheap.com) (~$10/год)
- [nic.ru](https://nic.ru) (~500₽/год)

---

**Всё готово!** 🚀 Ваш сайт в интернете!

